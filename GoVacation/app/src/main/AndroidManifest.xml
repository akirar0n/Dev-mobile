<?xml version="1.0" encoding="utf-8"?>
<!--
    ╔══════════════════════════════════════════════════════════════════╗
    ║  GoVacation — AndroidManifest.xml                               ║
    ║  Estas são as linhas que precisam existir no seu Manifest        ║
    ╚══════════════════════════════════════════════════════════════════╝
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.govacation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoVacation"
        android:networkSecurityConfig="@xml/network_security_config">

    <activity android:name=".MainActivity" android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".TelaCadastro" />
    <activity android:name=".GerenciamentoLocs" />
    <activity android:name=".TelaHome" />
    <activity android:name=".DetalhesReserva" />
    <activity android:name=".CadastrarLocs" />
    <activity android:name=".ListarLocs" />
    <activity android:name=".AlterarLocs" />

    </application>

    </manifest>